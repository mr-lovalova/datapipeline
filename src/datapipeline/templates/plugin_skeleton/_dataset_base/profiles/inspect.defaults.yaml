# See ../../reference/reference/tasks/profiles/inspect.reference.yaml for full options.
# Defaults for all inspect.<name>.yaml profiles in this project.
# precedence: CLI > inspect.<name>.yaml > inspect.defaults.yaml > built-ins
cmd: inspect
# artifact_mode: AUTO
# output:
#   transport: stdout
#   format: txt
observability:
  visuals: ON
  heartbeat_interval_seconds: 60
  logging:
    level: INFO
    outputs:
      - transport: STDERR
