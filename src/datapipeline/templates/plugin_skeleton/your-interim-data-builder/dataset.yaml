# Early-stage dataset definition (record/feature prep only).
# Populate stream entries with your canonical stream ids.

sample:
  cadence: ${group_by}
  keys: []

features:
  - id: first_feature
    stream: your.stream.one
    field: some_field
