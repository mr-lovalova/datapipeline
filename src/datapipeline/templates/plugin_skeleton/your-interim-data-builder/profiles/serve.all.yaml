# See ../../reference/reference/tasks/profiles/serve.reference.yaml for full options.
type: serve
name: all
target: serve
enabled: true
build:
  mode: AUTO
keep: null
output:
  transport: fs
  format: jsonl
  directory: data/interim/jerry
