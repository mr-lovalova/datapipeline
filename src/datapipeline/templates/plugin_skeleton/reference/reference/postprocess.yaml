# Postprocess config reference.
# Uncomment only the policies the dataset needs.

# columns:
#   features:
#     threshold: 0.9
#     ids: [feature_id]       # optional; defaults to every feature
#   targets:
#     threshold: 0.9

# samples:
#   features:
#     threshold: 1.0
#     ids: [feature_id]       # optional
#   targets:
#     threshold: 1.0
#     ids: [target_id]        # optional
