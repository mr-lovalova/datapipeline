# Scaler task reference (all options).
# This file is documentation only; uncomment the keys you want to use.
#
# version: 1                # optional
# id: scaler

# entrypoint: core.build.scaler # optional; build runner id
#
# output: build/scaler.json  # optional; relative to project.paths.artifacts
# dependencies: []           # optional; artifact task ids this task depends on
# split_label: train         # optional; split label from globals.split
