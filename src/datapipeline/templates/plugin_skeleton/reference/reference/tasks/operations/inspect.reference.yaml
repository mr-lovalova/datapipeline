# Inspect operation task reference (all options).
# This file is documentation only; uncomment the keys you want to use.
#
# version: 1                            # optional
# id: matrix                            # any unique inspect operation id
#
# entrypoint: core.inspect.coverage     # core.inspect.coverage | core.inspect.thresholds | core.inspect.matrix
# dependencies:
#   - stats
#   - metadata
# options:                              # operation-specific options (all optional)
#   sort: missing                       # coverage/thresholds sort key: missing | nulls
#   threshold: 0.95                     # thresholds used by coverage/thresholds
#   quiet: true                         # suppress operation stdout (useful for matrix)
#   format: html                        # matrix only: html | csv
#   output: ./artifacts/inspect.html    # matrix/partitions output path override
