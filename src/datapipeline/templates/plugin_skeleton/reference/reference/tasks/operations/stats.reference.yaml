# Stats task reference (all options).
# This file is documentation only; uncomment the keys you want to use.
#
# version: 1                 # optional
# id: stats
#
# entrypoint: core.build.stats # optional; build runner id
#
# output: build/stats.json   # optional; relative to project.paths.artifacts
# dependencies:              # explicit artifact prerequisites used to collect stats
#   - metadata
#   - scaler
# mode: final                # required; final | raw
