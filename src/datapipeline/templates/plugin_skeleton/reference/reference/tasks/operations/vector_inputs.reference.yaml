# Vector inputs task reference (all options).
# This file is documentation only; uncomment the keys you want to use.
#
# version: 1                # optional
# id: vector_inputs
# kind: artifact            # required; runtime | artifact

# entrypoint: core.artifact.vector_inputs # optional; build runner id
#
# output: build/vector_inputs/manifest.json # optional; relative to project.paths.artifacts
# format: jsonl.gz          # optional; currently only "jsonl.gz"
