# Per‑artifact settings (what/where to build).
# Shared runtime knobs (visual provider/progress style/log_level/mode) live in runtime.build.yaml next to project.yaml.
# Override there; do not add visual provider/progress style/log_level here.
kind: partitioned_ids
output:
  path: expected.txt   # relative to project.paths.artifacts
include_targets: false
