# See ../reference/reference/postprocess.yaml for full options.
- drop:
    axis: vertical
    payload: targets
    threshold: 0.9

- drop:
    axis: vertical
    payload: features
    threshold: 0.9

- drop:
    axis: horizontal
    payload: features
    threshold: 1

- drop:
    axis: horizontal
    payload: targets
    threshold: 1
