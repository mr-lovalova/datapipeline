# See ../../reference/reference/tasks/profiles/build.reference.yaml for full options.
# Defaults for all build.<name>.yaml profiles in this project.
# precedence: CLI > build.<name>.yaml > build.defaults.yaml > built-ins
cmd: build
# mode: AUTO
# observability:
#   visuals: OFF
#   logging:
#     level: INFO
