# See ../reference/reference/dataset.yaml for full options.

sample:
  cadence: ${group_by}
  keys: []

features:
  - id: first_feature
    stream: your.stream.one
    field: some_field
    scale: true
    sequence: { size: 6, stride: 1 }

  - id: second_feature
    stream: your.stream.two
    field: some_field

# targets:
#   - id: target_feature
#     stream: your.target.stream
#     field: some_field
