# See ../../../plugin_skeleton/reference/reference/tasks/profiles/inspect.reference.yaml for full options.
# Defaults for all inspect.<name>.yaml profiles in this project.
# precedence: CLI > inspect.<name>.yaml > inspect.defaults.yaml > built-ins
cmd: inspect
# build:
#   mode: AUTO
# output:
#   transport: stdout
#   format: txt
observability:
  visuals: ON
  logging:
    level: INFO
    outputs:
      - transport: STDERR
