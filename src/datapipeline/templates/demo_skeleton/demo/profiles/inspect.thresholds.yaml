# See ../../../plugin_skeleton/reference/reference/tasks/profiles/inspect.reference.yaml for full options.
cmd: inspect
name: thresholds
order: 70
target: thresholds
enabled: true
output:
  transport: stdout
  format: txt
