id: time.ticks.linear
from:
  stream: time.ticks.linear.ingest

cadence: ${group_by}

stream:
  - dedupe: {}
  - granularity: { field: value, mode: first }
  - ensure_cadence: { field: value, cadence: "${cadence}" }

debug:
  - lint: { mode: error, tick: "${cadence}" }
