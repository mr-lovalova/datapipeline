id: equity.msft
from:
  stream: equity.msft.ingest

cadence: ${group_by}

stream:
  - operation: dedupe
  - { operation: collapse, keep: first }
  - operation: rolling
    field: dollar_volume
    to: adv5
    window: 5
    statistic: mean
    min_samples: 3
