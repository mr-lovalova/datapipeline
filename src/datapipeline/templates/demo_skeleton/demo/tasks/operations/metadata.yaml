# See ../../../../plugin_skeleton/reference/reference/tasks/operations/metadata.reference.yaml for full options.
id: metadata
kind: artifact
entrypoint: core.artifact.metadata
dependencies:
  - schema
