id: metrics.humidity.ingest
from:
  source: regression.humidity
partition_by: location
feature_id_by: location

map:
  entrypoint: identity
  args: {}

record:
  - where: { operator: ge, field: time, comparand: "${start_time}" }
  - where: { operator: le, field: time, comparand: "${end_time}" }
