id: metrics.production.wind
from:
  stream: metrics.production.wind.ingest

partition_by: [municipality]

stream:
  - { operation: ensure_cadence, cadence: 1h }
  - { operation: fill, statistic: median, window: 3, min_samples: 1, field: value }
