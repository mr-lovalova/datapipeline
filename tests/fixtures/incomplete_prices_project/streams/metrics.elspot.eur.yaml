id: metrics.elspot.eur
from:
  stream: metrics.elspot.eur.ingest

partition_by: area
feature_id_by: area

stream:
  - ensure_cadence: { cadence: 1h, field: value }
  - fill: { method: mean, window: 2, min_samples: 1, field: value }
