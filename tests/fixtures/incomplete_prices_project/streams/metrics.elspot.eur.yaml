id: metrics.elspot.eur
from:
  stream: metrics.elspot.eur.ingest

partition_by: [area]
feature_id_by: [area]

stream:
  - { operation: ensure_cadence, cadence: 1h }
  - { operation: fill, statistic: mean, window: 2, min_samples: 1, field: value }
