id: regression.signal
parser:
  entrypoint: core.temporal.csv
  args:
    time_field: time
    value_field: value
loader:
  transport: fs
  path: data/signal.csv
  reader:
    format: csv
    encoding: utf-8
    delimiter: ","
